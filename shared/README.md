# Shared Contract

`scene-schema.json` describes the canonical `SceneSpec` shared between the MuseWave frontend editor and backend export pipeline.

The backend is the source of truth for canonical scene normalization, but the frontend mirrors the same shape for optimistic editing and playback.
